# 基线与首个边界修复

基线提交 `86c8014eb2db6789ea6ff3d9fd6f210648d911c3`，工作区原本干净。所有测试使用临时隔离目录和合成材料，没有读取用户业务数据。

## 修改前复现

- `.venv/bin/python -m pytest tests/test_golden_pipeline.py tests/test_api_agent.py tests/test_runtime_model_boundaries.py`：19 passed，30.03 秒；包括真实 WorkerProcessRunner 正常训练、人工拒绝、包重载评分、事件／检查点。
- `.venv/bin/python -m pytest tests/test_evaluation_contract.py::test_reviewer_block_cannot_be_auto_approved_in_fully_trusted_mode`：1 passed，2.20 秒。此项使用 Fake Provider 验证审核阻断，不是真实模型效果。
- 正常训练使用本地真实统计模型，LLM 关闭；不能作为自主 Agent 效果基线。阶段等待和调用的细分测量将由后续隔离演示脚本输出。
- 现有 train_and_review 最多重复三次审核训练，未形成效果诊断和 PatchPlan；train_candidates 每次计算 OOT，且对包含 OOT 的全表预测评分分布。

## 首个修复

训练函数支持显式 `evaluate_oot=False`：开发阶段只预测 Train 和开发验证集 Test；最终冻结后独立调用 `evaluate_final_holdout`。保留单次训练 API 的历史默认行为，产品优化循环必须显式关闭它。

验证：`tests/test_modeling_v11.py` 2 passed（0.40 秒）；新增访问观测证明开发阶段不预测 OOT，最终检验只允许一次。后续必须通过产品编排集成证明整个循环的边界。
