# 全仓代码复查与修复记录

日期：2026-09-09；检查起点 `2814dde`，修复在 `codex/risk-autonomous-optimization`。本文件的“修复”指代码实现，不代替实际运行证明。

## 范围与方法

清点第一方后端、前端、桌面壳、测试、发布与 CI 文件，见 [文件清单](2026-09-09-file-inventory.md)。清单是审查起点的文件指纹索引；修复后的最终版本由 Git 提交固定。共 271 个文件、47,280 行的入口、依赖与调用边界复查；针对变更链路逐段追踪状态、输入、授权、进程、产物和异常路径，结合全量测试及打包契约检查。并未宣称全部文件逐行证明正确，也未审计第三方依赖实现。排除 node_modules、虚拟环境、生成构建物、真实业务数据和本地秘密。

## 发现与处理

| ID / 等级 | 触发及影响 | 修复入口与处理 | 验证依据 |
|---|---|---|---|
| ROOT-R01 / P2 | AUC 略增但小于继续搜索阈值时，历史最佳没有更新 | `app/services/pipeline.py:train_and_review` 分离严格最佳与 meaningful_improvement；改善即保存，是否继续由耐心计数控制 | `tests/test_optimization.py` 的变差、0.0005 微增、显著提升参数化检查 |
| AUD-R02 / P1 | 比较只核对原始数据，清洗后数据不同仍可能标可比 | `pipeline.py:finalize_model_plan` 冻结 working_data_sha256；`run_readiness.py:compare_runs` 缺失或不一致拒绝直接比较 | 同原始来源、不同工作数据回归 |
| AUD-R03 / P1 | 只预留输出 Token，可在输入很大或 usage 缺失时穿过预算 | `pipeline.py:_gateway` 保守预留输入与输出，账本采用实际/预留最大值；调用前检查单次和月累计 | 请求尚未联网即预算拒绝；未知用量不按零消费释放预留 |
| AUD-R04 / P1 | 产品 Worker 未收到评测临时密钥，真实轨可能实际降级 | `process_runner.py` 私有进程参数传递；`evaluation/adapter.py` 配置轨强制有限 run_token_budget | 临时测试值到达 Pipeline，未进入公共 state/result；不含真实密钥 |
| AUD-R05 / P1 | 杀父进程未确认模型后代，超时后可能继续消耗资源 | `process_runner.py:_terminate` 后代 terminate/kill/wait，无法检查时显式失败；剩余计算预算进入硬超时 | 后代顺序检查及真实写入子进程被超时终止；原有评测超时／取消实进程检查 |
| AUD-R06 / P2 | Worker 内部冠军与目标约束选中冠军不同，评分上下限统计可能指错模型 | `pipeline.py:train_and_review` 按实际选中 bundle 重算开发期评分统计 | 最佳保留回归；最终包重载同样本一致性 |
| AUD-R07 / P2 | 聊天重训可能沿用未修改前的初始请求目标 | `conversations.py` 优先继承已经确认的目标快照 | 聊天真实创建与幂等回执回归；新运行产生独立快照 |
| AUD-R08 / P2 | 限流故障没有明确独立重试记录 | `providers/gateway.py:complete_json` 只对明确 429 做一次有独立 purpose 的重试，仍经过预算；不自动重试结果不明的超时 | 两次 rate-limit 调用后停止；无无限重试 |
| AUD-R09 / P1 | 多个工具可各耗完整时间配额 | `orchestration/graph.py` 累计工具计算秒数，剩余额传入真实 Worker；预算错误结束当前 Span | 完整回归及真实硬超时；比较输出 total_compute_seconds |

原有健康接口空 run_id、架构分层依赖问题已在 `2814dde` 修复。首轮全量 271 通过、5 失败：3 项代码问题修复，2 项为受限进程树检查环境问题；后者用真实权限复验 2 通过，未改断言绕过。

## 验证状态与限制

- 修复期间一次全量 282 项通过，193.84 秒；之后新增微增最佳与真实硬终止检查，最终稳定快照结果在 [最终验收](../evidence/2026-09-09-acceptance.md) 记录。
- Ruff 检查与格式检查通过；打包契约 `risk-packaging-contract/v2` 和桌面契约静态检查 valid=true。这不是 Windows 安装包实机通过。
- 前端 90 项回归、对比度、类型、Lint、格式和生产构建通过；工作台有本地合成运行与聊天重训实际页面证据。
- 外部费用以 Token 硬上限保守控制，费用货币值缺价格表时保持 null。不能将其宣传为精确美元费用管理。
- 硬预算中止可终结为失败，已完成轮次证据仍保留；不会为交付假装超时任务已经成功。人工等待不计累计工具秒数，但优化段另有从方案冻结起计的墙钟期限，长期等待后继续可能安全超时。
- 中断中存在潜在付费／训练副作用时不盲目重放，安全阻断并要求新运行。并非所有异常都能自动恢复。
- 本次没有进行真实 Provider 多次波动、真实信贷数据、真实用户或 Windows/macOS 安装器实机验收。需分别提供有限调用预算、合规数据与设备后验证。

## 验收收尾补充

- `4e9bc54` 修复预检计算了划分却返回旧占位字段的缺口，并增加月Token耗尽预检和前端可读资源说明；相关10项通过。
- `c1abbf5` 提供 `risk-eval-budget/v1` 能力：同Run Planner/Reviewer共享Token预留账本，OpenAI/Anthropic usage缺失均保持null；已通过无网络跨角色预算回归及原Provider边界16项、评测合同10项。单Run产品执行器串行；能力不承诺跨独立进程同时操作同工作区的分布式配额管理。
- 新增R01真实初始达标不重训、R09有效manifest下副作用不明禁止重放两项通过。最终稳定回归以根目录交接记录为准。
